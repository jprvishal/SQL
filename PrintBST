
SELECT N, 'Root'
FROM BST
WHERE P IS NULL
UNION
SELECT N, 'Leaf'
FROM BST
WHERE N NOT IN (SELECT P FROM BST WHERE P IS NOT NULL)
UNION
SELECT N, 'Inner'
FROM BST
WHERE N IN (SELECT N FROM BST WHERE N IN (SELECT P FROM BST WHERE P IS NOT NULL) AND P IS NOT NULL);
